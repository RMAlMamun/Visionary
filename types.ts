export const GenerationMode = {
  TEXT_TO_IMAGE: 'TEXT_TO_IMAGE',
  IMAGE_TO_IMAGE: 'IMAGE_TO_IMAGE',
  TEXT_TO_VIDEO: 'TEXT_TO_VIDEO'
};

// JavaScript does not use Interfaces or Type definitions.
// Previous interfaces (GeneratedContent, AspectRatio, etc.) are removed as they are not needed at runtime.